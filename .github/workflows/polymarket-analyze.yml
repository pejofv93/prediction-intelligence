name: polymarket-analyze
on:
  schedule:
    - cron: '0 4,10,16,22 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Enrich markets (prereq de analyze)
        continue-on-error: true
        run: |
          curl -X POST "${{ secrets.POLY_AGENT_URL }}/run-enrich" \
            -H "Content-Type: application/json" \
            -H "x-cloud-token: ${{ secrets.CLOUD_RUN_TOKEN }}" \
            --max-time 600 \
            --fail --silent --show-error
      - name: Analyze markets
        run: |
          curl -X POST "${{ secrets.POLY_AGENT_URL }}/run-analyze" \
            -H "Content-Type: application/json" \
            -H "x-cloud-token: ${{ secrets.CLOUD_RUN_TOKEN }}" \
            --max-time 300 \
            --fail --silent --show-error
